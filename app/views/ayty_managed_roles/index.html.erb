<!-- ##### AYTYCRM - Silvio Fernandes ##### -->
<h2><%=l(:label_ayty_managed_roles_plural)%></h2>

<table class="list">
  <thead><tr>
    <th><%=l(:label_role)%></th>
  <th></th>
  </tr></thead>
  <tbody>
<% for role in @roles %>
  <tr class="<%= cycle("odd", "even") %>">
    <td class="name"><%= content_tag(role.builtin? ? 'em' : 'span', link_to(role.name, edit_ayty_managed_role_path(role))) %></td>
  </tr>
<% end %>
  </tbody>
</table>

<p class="pagination"><%= pagination_links_full @role_pages %></p>

<% html_title(l(:label_ayty_managed_roles_plural)) -%>
